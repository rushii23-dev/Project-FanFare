; eslint-plugin-jsx-a11y declares peer eslint ^3–^9 but is flat-config
; compatible and runs cleanly on ESLint 10 — only its peer range lags the
; ESLint release. Remove once the plugin publishes an eslint@10 peer range.
legacy-peer-deps=true
