node_modules
dist
dist-ssr
.vite

# Secrets. *.local covers .env.local, which holds the real GEMINI_API_KEY.
# Never remove this rule.
*.local
.env.example

# Agent tooling — local only
.claude/
.agents/

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
Thumbs.db
